# RELEASES

Shippable release files — committed and tagged, not build cruft.

The site has no build step, so a release is the tagged contents of `src/`
uploaded to the host. Keep any packaged deploy archives handed to the hosting
provider here.
