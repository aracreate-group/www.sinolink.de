# TESTS

Checks and one-off prototypes for the site. The asset-reference check run by
`make test` lives in `scripts/check-assets.sh`; add page-level, accessibility
or link-checking tests here as they are written.
