# ARCHIVES

Approaches trialled during development but not used in the released version.
Keep them documented here — what was tried and why it was dropped — so the
knowledge is not lost, without cluttering `src/`.

Currently empty. The partner-strip layout trials that once lived here were not
dead ends but working design references, and have been written up as Markdown in
`docs/design-references/`.
